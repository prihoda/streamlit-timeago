import React from "react"
import ReactDOM from "react-dom"
import StreamlitTimeAgo from "./StreamlitTimeAgo"

ReactDOM.render(
  <React.StrictMode>
    <StreamlitTimeAgo />
  </React.StrictMode>,
  document.getElementById("root")
)
